# تصحيح مهم: خوارزمية ترخيص الهاتف كانت خاطئة تماماً — تم إصلاحها الآن

## ماذا حدث
الأرشيف الشامل الذي أرسلته سابقاً بنى ترخيص الهاتف بخوارزمية **HMAC-SHA256** بناءً على نسخة قديمة/أبسط من `license.cjs` كانت موجودة في بيئتي فقط. بعد أن أرسلت لي المحتوى **الحقيقي** لملفك، تبيّن أنه يستخدم فعلياً **Ed25519 (توقيع رقمي غير متماثل)** بصيغة كود مختلفة تماماً (`AGRI2.<payload>.<signature>` بدل `AGRI-TYPE-DEVICE8-EXPIRY-SIG`). لو استخدمت الملف القديم، **كل كود تفعيل حقيقي كان سيُرفَض بالكامل.**

## الإصلاح
أعدت بناء `src/lib/mobileLicense.ts` من الصفر ليطابق `license.cjs` الحقيقي **حرفياً**: نفس `PRODUCT_ID` (`PROD-002`)، نفس المفتاح العام (استخرجته من نفس ملف PEM الذي أرسلته وتحققت من الاستخراج بايتاً بايت)، نفس صيغة الكود بثلاثة أجزاء، نفس حقول الحمولة السبعة، نفس منطق التحقق بالضبط.

## لماذا مكتبة `@noble/ed25519` وليس التنفيذ المدمج في المتصفح
دعم خوارزمية Ed25519 داخل Web Crypto API للمتصفحات **متفاوت وغير مضمون** عبر كل إصدارات WebView أندرويد الموجودة فعلياً على أجهزة عملائك. `@noble/ed25519` مكتبة JS بحتة (لا تعتمد على دعم المتصفح لهذه الخوارزمية تحديداً)، مستخدَمة على نطاق واسع، هذا يضمن عملها بشكل متطابق على أي جهاز.

## التحقق الذي أجريته قبل الإرسال
- استخرجت المفتاح الخام من ملف PEM الذي أرسلته، وتحققت **بايتاً بايت** أن الاستخراج صحيح 100%.
- ولّدت كود تفعيل حقيقي فعلياً (بمفتاح Ed25519 حقيقي، بنفس منطق `license.cjs` بالضبط) واختبرت **كل خطوة تفكيك الكود** (التقسيم لثلاثة أجزاء، فك ترميز base64url، تفكيك 7 حقول الحمولة، طول التوقيع 64 بايت) — كلها مطابقة تماماً لما يتوقعه الكود الحقيقي.
- الجزء الوحيد غير القابل للاختبار هنا فعلياً: دالة `verifyAsync` نفسها من مكتبة `@noble/ed25519` (لا يمكنني تثبيت حزم npm في بيئتي بلا إنترنت) — لكنها مكتبة معروفة وموثوقة على نطاق واسع، وكل ما حولها تحقق بدقة.

## إضافة واحدة مطلوبة في package.json
```
    "@noble/ed25519": "^2.1.0",
```
أضفها داخل قسم `dependencies` (لم تعد تحتاج أي شيء آخر خاص بالترخيص — لا توجد حزمة إضافية لـ HMAC كما كان سابقاً لأن ذلك النهج أُلغي بالكامل).

## ماذا تفعل الآن
استبدل هذين الملفين في الأرشيف الشامل الذي أرسلته سابقاً بالنسختين المرفقتين هنا:
- `src/lib/mobileLicense.ts`
- `src/store/AppContext.tsx`

(باقي الـ23 ملفاً من الأرشيف الشامل السابق يبقون كما هم بدون أي تغيير)

## التحقق العام
22/22 اختبار محاسبي، وفحصت صياغة كلا الملفين بدقة.
