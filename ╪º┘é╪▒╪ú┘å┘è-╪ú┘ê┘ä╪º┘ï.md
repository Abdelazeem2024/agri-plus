# المساعد الذكي — المرحلة الأولى (كتابة نصية كاملة الآن، الصوت خطوة تالية)

6 ملفات: 2 جديدة، 4 تعديل.

## ⚠️ مهم جداً — اقرأ هذا أولاً
بيئة العمل هنا **بلا اتصال إنترنت إطلاقاً**، لذلك لا يمكنني تحميل ملف نموذج whisper.cpp الفعلي (75 ميجا) ولا الملف التنفيذي المُجمَّع. لذلك بنيت **كل ما هو كود بحت** بالكامل الآن (وهو الجزء الأكبر والأهم فعلياً)، وتركت الإدخال الصوتي **مُعطَّلاً تلقائياً وبأمان** (الزر يظهر رمادياً مع تلميح توضيحي) حتى تُستكمل خطوة تحميل الملفات الثنائية لاحقاً على جهاز متصل بالإنترنت. **الكتابة النصية تعمل الآن بكامل قوة الفهم — لا فرق في القدرة، فقط طريقة الإدخال.**

## الملفات

| الملف | النوع | الوصف |
|---|---|---|
| `src/lib/aiEngine.ts` | **جديد** | محرك الفهم الكامل (بدون إنترنت، بدون أي نموذج ضخم) |
| `src/pages/AIAssistant.tsx` | **جديد** | شاشة المحادثة + عرض النتائج كتقارير احترافية |
| `src/types/index.ts` | تعديل | إعادة إضافة حقل `refundAmount` (قد يكون موجوداً بالفعل في نسختك) |
| `src/App.tsx` | تعديل | مسار `/ai-assistant` |
| `src/components/Sidebar.tsx` | تعديل | رابط "المساعد الذكي" بلون مميز (ذهبي/أخضر) |
| `src/pages/Dashboard.tsx` | تعديل | **الزر/البطاقة الاحترافية** التي طلبتها في أعلى الصفحة الرئيسية |

## كيف يفهم المحرك (بدون إنترنت، بدون نموذج ضخم)
1. **تطبيع النص العربي**: يوحّد كل أشكال الألف/التاء المربوطة/الياء قبل أي مقارنة.
2. **تصنيف النية**: مطابقة كلمات مفتاحية ذكية (أرباح، تحصيلات، مخزون...) + عبارة "فلان عليه كام؟" الطبيعية كحالة احتياطية عامة.
3. **المطابقة التقريبية للأسماء** (Levenshtein): يتحمّل الأخطاء الإملائية، ويحدد تلقائياً هل الاسم المذكور عميل أم مندوب.
4. **التنفيذ الفعلي**: يحسب من بياناتك الحقيقية مباشرة (نفس الصيغ المحاسبية المستخدمة في باقي البرنامج بالضبط).
5. **لا تخمين**: إذا لم يفهم، يطلب توضيحاً صراحة بدل تخمين إجابة قد تكون خاطئة.

## اختبار حقيقي أجريته (وليس افتراضياً)
شغّلت 12 جملة عامية مصرية طبيعية فعلياً، ووجدت 3 أخطاء حقيقية في المحاولة الأولى (مثل عدم فهم "احمد شوقي عليه كام؟" لأنه لا يحتوي كلمة "عميل" صراحة، والتباس "رصيد" بين مديونية ومخزون) — **أصلحتها جميعاً وأعدت الاختبار: 12/12 ناجحة**، شاملة الرفض الصحيح لجملة غير متعلقة ("عايز اطلب بيتزا").

## الخطوة التالية (تحتاج جهازاً متصلاً بالإنترنت)
لتفعيل الإدخال الصوتي فعلياً، يلزم لاحقاً: تحميل `whisper.cpp` (نموذج tiny، 75 ميجا فقط، مجاني ومفتوح المصدر) وربطه عبر Electron. الواجهة والمنطق جاهزان بالكامل لاستقباله بمجرد توفره — فقط أخبرني عندما تريد ذلك في محادثة قادمة وسأكتب لك التكامل الكامل.

## التحقق
22/22 اختبار محاسبي ناجح (بلا أي كسر)، وفحصت صياغة كل الملفات الستة.
