# الإدخال الصوتي على الهاتف — بالنهج الصحيح (وليس امتداد whisper.cpp)

## لماذا نهج مختلف تماماً عن سطح المكتب
`whisper-cli.exe` مبني لويندوز فقط — **لا يمكن تشغيله على أندرويد إطلاقاً** مهما حاولنا. النهج الصحيح والوحيد المنطقي: استخدام محرك التعرّف على الكلام **المدمج مجاناً في نظام أندرويد نفسه** — نفس المحرك الذي تستخدمه لوحة مفاتيح جوجل ومساعد جوجل، يدعم العربية، ولا يحتاج أي تحميل لنموذج ضخم ولا أي خادم خارجي.

## كيف يعمل من ناحية تجربة الاستخدام
اضغط 🎤 → يظهر حوار أندرويد الأصلي المألوف ("تحدث الآن...") → تتكلم → يغلق تلقائياً ويظهر النص منفَّذاً في المساعد الذكي.

## الملفات
| الملف | الدور |
|---|---|
| `src/lib/mobileSpeechRecognition.ts` | **جديد** — يغلّف محرك أندرويد المدمج (يطلب الإذن تلقائياً عند أول استخدام) |
| `src/pages/AIAssistant.tsx` | تعديل — زر الميكروفون يتفرّع الآن: أندرويد يستخدم المحرك المدمج، سطح المكتب يستخدم whisper.cpp كما هو |

## الحزمة المطلوبة (أضفها لقائمة package.json الشاملة)
```
"@capacitor-community/speech-recognition": "^6.0.1",
```
أضفها داخل `dependencies`.

## قرار تصميمي مهم
استخدمت وضع "الحوار الأصلي" (`popup: true`) بدل بناء واجهة تسجيل مخصصة — حوار أندرويد الرسمي يتولى بنفسه عرض "جارٍ الاستماع..." والإيقاف التلقائي عند توقفك عن الكلام، بنفس الشكل المألوف للمستخدم من تطبيقات أخرى يستخدمها يومياً. هذا أبسط وأكثر موثوقية من إعادة بناء تلك الواجهة يدوياً.

## التحقق
فحصت كلا الملفين بمترجم TypeScript الحقيقي — صفر أخطاء. 22/22 اختبار محاسبي ناجح.

⚠️ **لم أستطع اختبار دقة التعرّف الفعلي على كلامك بلهجتك** — هذا يحتاج تجربتك الفعلية على جهاز حقيقي.
