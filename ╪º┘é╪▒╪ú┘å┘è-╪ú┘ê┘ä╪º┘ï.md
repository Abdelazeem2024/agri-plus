# ملف معدَّل واحد فقط — نظام الربط بين برنامج العميل وبرنامج التفعيل

| المسار في المستودع | نوع التعديل |
|---|---|
| `electron/license.cjs` | تعديل: إضافة `PRODUCT_ID` صريح داخل السر الموقَّع |

## ⚠️ مهم قبل الرفع
هذا التعديل غيّر "السر" (Secret) المستخدم في توقيع الأكواد. **أي كود تفعيل قديم كنت قد ولّدته بالنسخة السابقة من هذا الملف لن يعمل بعد هذا التحديث** — يجب توليد أكواد جديدة بعده. بما أنك ذكرت أنك لم تبدأ البيع بعد، هذا هو التوقيت الصحيح لهذا التعديل قبل تسليم أي نسخة للعملاء.

## لماذا هذا التعديل تحديداً؟
راجعت طلبك بالكامل نقطة نقطة، وكل ما طلبته **موجود بالفعل ويعمل** في الكود الحالي:

| المطلوب | الحالة |
|---|---|
| إنشاء Machine Fingerprint | ✅ موجود (`getMachineId` في `license.cjs`) |
| زر نسخ البصمة | ✅ موجود (`Settings.tsx` سطر 198) |
| شاشة إدخال كود التفعيل | ✅ موجودة (`Settings.tsx`) |
| التحقق محلياً بدون إنترنت | ✅ HMAC-SHA256 محلي بالكامل، لا يوجد أي اتصال خارجي |
| ربط الكود بالجهاز (Machine Fingerprint) | ✅ مُتحقَّق ومُختبَر (`reject key for wrong machine` في مجموعة الاختبارات) |
| ربط الكود بمنتج هذا البرنامج فقط | ⚠️ كان **ضمنياً** فقط (عبر السر الثابت) — **تم توضيحه صراحة الآن** عبر `PRODUCT_ID` |
| كود صحيح → يفتح البرنامج / كود خطأ → يُرفض | ✅ (`LicenseGate` في `App.tsx`) |
| حفظ حالة التفعيل محلياً | ✅ (SQLite + localStorage) |
| Trial 3 أيام إن لم يُفعَّل | ✅ موجود ويعمل |
| إعادة تثبيت على نفس الجهاز يقبل نفس الترخيص | ✅ التحقق بدون حالة مركزية (Stateless) — نفس الكود يعمل مجدداً طالما نفس الجهاز |
| منع كود تابع لبرنامج آخر | ⚠️→✅ تم تقويته بـ `PRODUCT_ID` الصريح في هذا التعديل |
| منع كود تابع لجهاز آخر | ✅ مُتحقَّق ومُختبَر |

اختبرت التعديل عملياً: ولّدت كوداً مزوَّراً بسرّ مختلف (يحاكي "برنامج آخر") وتأكدت أن النظام يرفضه برسالة "التوقيع غير صالح — كود مزور أو تالف".

## نقطة تربط كل هذا معاً (لم تحتج تعديلاً، فقط للتوضيح)
أداة توليد الأكواد `license-generator/generate.cjs` (وهي "برنامج التفعيل" الذي تستخدمه أنت كبائع) **تستورد نفس ملف `electron/license.cjs` مباشرة** — أي أنها والتطبيق نفسه يستخدمان **نفس السر تلقائياً** بدون أي تكرار أو احتمال عدم تطابق. هذا هو الرابط الذي سألت عنه، وهو مضمون بنيوياً (Structurally) منذ تصميم النظام الأصلي.
