# ملفات معدَّلة — Agri Plus

طريقة الاستخدام: انسخ كل ملف من هذا الأرشيف إلى **نفس المسار بالضبط** في مستودع GitHub (استبدال الملف الموجود)، ما عدا ملف الخط فهو **جديد** يُضاف في مساره.

| المسار في المستودع | نوع التعديل | السبب |
|---|---|---|
| `src/lib/pdf.ts` | إعادة كتابة كاملة | محرك تصدير/طباعة التقارير الجديد: خط عربي مضمّن، تصميم احترافي، شعار الشركة |
| `electron/main.cjs` | تعديل | إصلاح تجمد الواجهة عند فتح أكثر من تقرير/فاتورة (`setWindowOpenHandler`) |
| `src/pages/Settings.tsx` | تعديل | رفع شعار الشركة + تصدير/استيراد أكثر أماناً (async + رسائل خطأ واضحة) |
| `src/pages/CustomerStatement.tsx` | تعديل | تمرير الشعار والعنوان للتقرير المُصدَّر |
| `src/pages/RepresentativeStatement.tsx` | تعديل | تمرير الشعار والعنوان للتقرير المُصدَّر |
| `src/pages/Reports.tsx` | تعديل | تمرير الشعار والعنوان للتقرير المُصدَّر |
| `src/assets/fonts/NotoNaskhArabic-Regular.ttf` | **ملف جديد** | خط عربي يُضمَّن داخل كل تقرير PDF |
| `src/vite-env.d.ts` | تعديل | تعريف نوع لاستيراد ملفات الخطوط (.ttf) لـ TypeScript |
| `src/db/storage.ts` | تعديل | ضمان اكتمال كل حقول التصدير/الاستيراد ومنع تعطل البرنامج بملف ناقص |
| `index.html` | تعديل | تطبيق الوضع الليلي فوراً عند بدء التشغيل (منع ومضة الشاشة البيضاء) |
| `src/store/AppContext.tsx` | تعديل | منع حذف عميل/مندوب/صنف له معاملات مرتبطة + تأكيد قبل حذف المندوب والصنف |
| `src/pages/RepresentativeReturns.tsx` | تعديل | إزالة نافذة تأكيد مكررة عند حذف مرتجع مندوب |
| `src/pages/CustomerReturns.tsx` | تعديل | إزالة نافذة تأكيد مكررة عند حذف مرتجع عميل |

## بعد النسخ إلى GitHub
شغّل الأوامر المعتادة لديك للبناء (`npm install` ثم أمر البناء الخاص بمشروعك)، فملف الخط الجديد يحتاج أن يكون ضمن عملية `npm install`/`build` العادية حتى يلتقطه Vite تلقائياً — لا حاجة لأي خطوة يدوية إضافية.
