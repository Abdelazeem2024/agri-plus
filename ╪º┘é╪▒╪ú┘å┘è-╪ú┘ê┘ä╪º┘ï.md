# نظام التفعيل الجديد — التوقيع الرقمي Ed25519

## ⚠️ الخطوة الإجبارية الأولى قبل أي بيع (لا تتخطاها)

أثناء تطوير هذا النظام واختباره، ولّدت زوج مفاتيح تجريبياً **ظهر مفتاحه الخاص في نص المحادثة**. أي شخص يقرأ تلك المحادثة يملك هذا المفتاح. لذلك ملف `electron/license.cjs` المُسلَّم إليك يحتوي على قيمة placeholder فقط:

```
REPLACE_ME_RUN_keygen.cjs_AND_PASTE_YOUR_OWN_PUBLIC_KEY_HERE
```

**يجب عليك تنفيذ هذا قبل تسليم أي نسخة لأي عميل:**

```bash
cd license-generator
node keygen.cjs
```

سيطبع لك الأمر مفتاحاً عاماً جديداً — انسخه بالكامل والصقه مكان الـ placeholder في `electron/license.cjs` (متغيّر `PUBLIC_KEY_PEM`). المفتاح الخاص المقابل يُحفظ تلقائياً في `license-generator/keys/default/private-key.pem` على جهازك فقط — **لا تشاركه مع أحد ولا ترفعه لأي مكان** (محمي فعلاً بواسطة `.gitignore` المرفق).

---

## كيف يعمل النظام الآن (مقابل القديم)

| | القديم (HMAC) | الجديد (Ed25519) |
|---|---|---|
| السر داخل برنامج العميل | نعم (يمكن استخراجه) | **لا يوجد إطلاقاً** |
| من يقدر يولّد أكواد صالحة | أي شخص يملك ملفات البرنامج | أنت فقط، عبر المفتاح الخاص على جهازك |
| حالة "مُفعَّل" في قاعدة البيانات | عمود true/false قابل للتعديل يدوياً | **يُعاد التحقق من توقيع الكود الكامل** في كل إقلاع، لا يُعتمَد على عمود وحده |
| ربط بالمنتج | ضمني (سر ثابت) | صريح (`productId` داخل البيانات الموقَّعة) |
| فحص سلامة الملفات | غير موجود | موجود (`electron/integrity.cjs`) |

## بنية النظام

```
electron/
  license.cjs         ← يُشحن مع البرنامج. يحتوي فقط على المفتاح العام (آمن للكشف)
  integrity.cjs        ← يكشف أي تعديل لاحق على ملفات التفعيل نفسها
  integrity-manifest.json  ← يُولَّد تلقائياً عند البناء (لا يُرفع لـ git)

license-generator/      ← يبقى على جهازك أنت فقط، لا يُشحن مع البرنامج أبداً
  keygen.cjs            ← تشغّله مرة واحدة لكل برنامج جديد
  generate.cjs          ← تشغّله لكل عميل لتوليد كود تفعيله
  keys/                 ← المفاتيح الخاصة (gitignored)
  issued/                ← سجل من حصل على أي ترخيص (gitignored)
```

## توليد كود لعميل جديد

1. العميل يفتح البرنامج → إعدادات → ينسخ "معرّف الجهاز" (32 حرفاً) ويرسله لك.
2. على جهازك:
   ```bash
   cd license-generator
   node generate.cjs <MACHINE_ID> PERM       # ترخيص دائم
   node generate.cjs <MACHINE_ID> YEAR 1     # ترخيص سنوي (سنة واحدة)
   ```
3. يطبع لك الأمر كوداً طويلاً يبدأ بـ `AGRI2.` — أرسله للعميل كاملاً (نسخ/لصق، وليس كتابة يدوية).
4. العميل يلصقه في شاشة الإعدادات → مساحة "كود التفعيل" → زر "تفعيل".

**ميزة تلقائية:** إذا طلب نفس العميل (نفس بصمة الجهاز) كوداً مرة أخرى لاحقاً (أعاد تثبيت البرنامج مثلاً)، يعيد له `generate.cjs` نفس تفاصيل ترخيصه الأصلي تلقائياً (نفس تاريخ أول تفعيل)، لأن السجل محفوظ محلياً في `license-generator/issued/`.

## التوسّع لبرنامج جديد مستقبلاً (مكتبات، إلخ)

1. `node keygen.cjs libraries-app` (مفتاح منفصل تماماً)
2. في نسخة كود برنامج المكتبات: غيّر `PRODUCT_ID` في `license.cjs` إلى مثلاً `LIBRARY-V1`، والصق المفتاح العام الجديد.
3. عند التوليد: `node generate.cjs <MACHINE_ID> PERM 1 libraries-app`

بهذا كل برنامج معزول تماماً عن الآخر تلقائياً — كود لأحدهما لن يعمل على الآخر مهما حدث.

## الحماية الإضافية الاختيارية (تمويه الكود)

طبقة اختيارية إضافية فوق التوقيع الرقمي (غير ضرورية للأمان الأساسي، لكنها ترفع كلفة أي محاولة قراءة يدوية للملفات):

```bash
npm install --save-dev javascript-obfuscator   # مرة واحدة فقط، يحتاج إنترنت
npm run electron:build:secure                  # بدل electron:build:win العادي
```

هذا الأمر يموّه `license.cjs` و`integrity.cjs` تلقائياً قبل التغليف، ثم يُرجعهما لصورتهما الأصلية القابلة للقراءة بعد الانتهاء (لصيانتك أنت لاحقاً). الاستخدام العادي `npm run electron:build:win` يبقى يعمل بدون هذه الخطوة إن أردت الإبقاء على الأمر بسيطاً — الحماية الجوهرية (التوقيع الرقمي) لا تعتمد على التمويه أصلاً.

## لماذا لم أستخدم native module مُصرَّف (C++)؟

طلبت أعلى حماية ممكنة **عملياً** مع بقاء الصيانة بسيطة. بما أن التوقيع الرقمي غير المتماثل يزيل جوهر المشكلة (لا يوجد سر يُستخرَج أصلاً)، فإن كتابة إضافة C++ (تحتاج Python + Visual Studio Build Tools على كل جهاز تبني عليه، وصيانة أصعب بكثير) تضيف تعقيداً كبيراً مقابل فائدة أمنية هامشية جداً في هذه الحالة تحديداً. إذا رغبت بها لاحقاً بعد نمو المشروع، يمكن إضافتها كطبقة منفصلة دون المساس بنظام التوقيع الحالي.

## ما الذي لا يزال يعتمد على حسن نية عدم وجود خادم؟

بصراحة تامة: أي حماية من طرف العميل فقط (بدون خادم مركزي) لها سقف نظري — شخص يملك مهارة عالية جداً ووقتاً كافياً يمكنه نظرياً تعديل ثنائيات Electron نفسها في وقت التشغيل (Runtime Patching بأدوات متقدمة). هذا مستوى مجهود غير عملي إطلاقاً لأي منافس محلي يستهدف محلات مبيدات صغيرة، وهو نفس السقف الذي تعمل ضمنه كل برامج سطح المكتب المدفوعة تقريباً. ما بنيناه هنا يرفع الجهد المطلوب من "فتح ملف نصي لثوانٍ" (الوضع القديم) إلى مستوى هندسة عكسية متقدمة جداً وغير مجدية اقتصادياً — وهو الهدف العملي الصحيح.
