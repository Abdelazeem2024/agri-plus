# ملفات معدَّلة — 5 نقاط (أهمها: الوضع الليلي بحل جذري)

12 ملفاً، جميعها تعديل.

## ⚠️ نقطة مهمة قبل أي شيء آخر
راجعت الصور المرفقة بدقة، ووجدت أن جدول "الحالة" بمفاتيح تبديل بيضاء الذي يظهر فيها **غير موجود إطلاقاً في الكود الحالي** (لا يوجد أي مفتاح تبديل/Toggle في أي مكان بالمشروع). هذا يعني أن الصور على الأرجح من نسخة تثبيت **أقدم** من كل التعديلات التي أرسلتها لك عبر هذه المحادثة. رغم ذلك، أخذت شكواك عن مشكلة "كل شيء يتحول للأبيض عند hover في الوضع الليلي" بجدية كاملة، وبحثت ووجدت **مشكلة حقيقية وممنهجة** في الكود الحالي تطابق نفس الوصف تماماً (تفصيل أدناه) — على الأرجح هي نفس المشكلة التي تراها، فأصلحتها بشكل جذري وشامل لكل الملفات.

## [1] الوضع الليلي — الحل الجذري
وجدت أن **كل أزرار التعديل/الحذف/كشف الحساب في كل جداول البرنامج تقريباً** (8 ملفات، 12 موضعاً) كانت تستخدم لون خلفية فاتح عند hover (`hover:bg-red-50`، `hover:bg-slate-100`، `hover:bg-blue-50`) **بدون أي نظير للوضع الليلي إطلاقاً**. أضفت `dark:hover:` لكل واحد منها.

بالإضافة لذلك، قوّيت بنية ملف `src/index.css` نفسه بشكل دفاعي: كانت هناك قاعدة عامة تفرض اللون الأبيض على كل `span/td/th/li` في الوضع الليلي **بدون أي استثناء**، وهذا بالضبط ما تسبب سابقاً في مشكلة "نص أبيض على خلفية بيضاء" في شارة حالة العميل. الآن أي عنصر منها له خلفية ملوّنة خاصة به (شارة/زر hover) يُستثنى تلقائياً من هذا الفرض، فلا يتكرر هذا النمط من الأخطاء في أي مكان آخر بالتطبيق مستقبلاً — حتى في أماكن لم أفحصها يدوياً.

**الملفات المتأثرة:** `Collections.tsx`, `CustomerReturns.tsx`, `EditInvoice.tsx`, `RepresentativeReturns.tsx`, `StockReceipts.tsx`, `Invoices.tsx`, `Products.tsx`, `Representatives.tsx`, `index.css`

## [2] صفحة مكافآتي — قسم التواصل
قوّيت التباين والخلفية بشكل كبير (من شفافية 20% إلى خلفية صلبة `bg-slate-800` في الوضع الليلي، مع حدود أوضح)، لضمان عدم وجود أي التباس بصري بغض النظر عن الإضاءة.

## [3] فلتر التاريخ في صفحة التقارير
أضفت شريط "من — إلى" أعلى الصفحة يؤثر فعلياً على **كل** الأرقام والجداول المعروضة (المبيعات، التحصيلات، المرتجعات، حركة المخزون) — ما عدا تقرير المخزون الحالي نفسه (لأنه رصيد لحظي، لا حركة زمنية). مع زر "إلغاء الفلتر" لإعادة عرض كل الفترات.

## [4] لوحة التحكم — أداء اليوم
أضفت شريطاً علوياً جديداً "أداء اليوم" (فواتير، مبيعات، تحصيلات، مرتجعات اليوم فقط) بتصميم مميز أخضر متدرج. يُحسب هذا تلقائياً بمطابقة تاريخ اليوم الحالي، فيبدأ من صفر تلقائياً كل يوم جديد بدون أي حاجة لآلية "تصفير" خاصة — أبقيت البطاقات الإجمالية القديمة كما هي أسفله لأنها مفيدة كنظرة عامة على أداء المحل الكلي.

## [5] التحقق من صحة المعادلات المحاسبية
شغّلت كامل حزمة الاختبارات الآلية بعد كل تعديل — **24/24 اختبار ناجح**، وفحصت صياغة كل الملفات الـ 12 (بما فيها ملف CSS) للتأكد من عدم وجود أي خطأ صياغي.

بعد الرفع: أعد البناء واختبر تحديداً صفحة المندوبين/العملاء/الفواتير في الوضع الليلي عند تمرير الماوس على أزرار التعديل والحذف، وفلتر التاريخ في التقارير.
