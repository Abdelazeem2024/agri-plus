# المرحلة الثالثة: قاعدة بيانات حقيقية بدل localStorage

## القرار الهندسي (ولماذا)
بدل إعادة بناء نفس الهيكل العلائقي المعقّد لقاعدة بيانات سطح المكتب (15 جدولاً مترابطاً)، اخترت تخزين البيانات كصف واحد يحتوي نص JSON كامل داخل جدول SQLite واحد بسيط. هذا قرار مقصود:
- كل منطق الفلترة والحسابات في التطبيق يحدث بالفعل داخل JavaScript، وليس عبر استعلامات SQL معقّدة — فلا فائدة حقيقية من التطبيع العلائقي على الهاتف.
- يحل المشكلتين الحقيقيتين لـ `localStorage` (السعة المحدودة ~5-10 ميجا، والموثوقية) لأن SQLite قاعدة بيانات حقيقية على القرص بسعة أكبر بكثير.
- يقلّل احتمال الأخطاء بشكل كبير مقارنة بكتابة واختبار CRUD كامل لـ 15 جدولاً بدون القدرة على اختبارها فعلياً هنا.

## الملفات
| الملف | النوع |
|---|---|
| `src/db/capacitorDb.ts` | **جديد** — التعامل المباشر مع SQLite على الهاتف |
| `src/db/storage.ts` | تعديل — إضافة "فرع" ثالث للتحميل/الحفظ (بجانب Electron وlocalStorage)، بنفس منطق المرونة المُثبَت في فرع Electron بالضبط |

⚠️ **`storage.ts` هنا نسخة بيئتي كاملة** — قبل الاستبدال، تأكد من نسختك الحالية (قد تحتوي تعديلات لم أرها). إن وجدت فرقاً، أرسل لي محتواها وسأدمج الإضافة بدقة بدل استبدال كامل.

## التحقق الذي أجريته (مهم لتفهم درجة الثقة الحقيقية)
✅ **اختبرت فعلياً** منطق المرونة الكامل بمحاكاة 4 سيناريوهات حقيقية:
1. أول تشغيل بلا أي بيانات — يعمل بأمان.
2. **الأهم**: عميل يُحدِّث من نسخة قديمة تستخدم localStorage فقط — البيانات المحلية **تُنقَل تلقائياً** لقاعدة SQLite الجديدة، **لا يحدث فقدان بيانات إطلاقاً**.
3. الاستخدام اليومي الطبيعي (قراءة من قاعدة بيانات بها بيانات فعلاً) — يعمل بشكل صحيح.
4. الحفظ — يعمل بشكل صحيح.

⚠️ **لم أختبره:** السلوك الفعلي لمكتبة `@capacitor-community/sqlite` نفسها على جهاز أندرويد حقيقي — بيئتي بلا إنترنت ولا جهاز حقيقي لتثبيت واختبار المكتبة الفعلية. تحققت من صحة الدوال المستخدَمة (أسماءها ومعاملاتها) من توثيق المكتبة الرسمي مباشرة قبل الكتابة، لكن الاختبار الحقيقي الوحيد يكون بعد بنائك للتطبيق وتجربته.

## إضافة واحدة مطلوبة في package.json
راجع الرسالة الرئيسية — سطر واحد فقط تضيفه بنفس الطريقة البسيطة التي نجحت معك آخر مرة.

## التحقق
22/22 اختبار محاسبي ناجح على الحالة الكاملة بعد الدمج.

## بعد أن تجرّب هذه المرحلة وتؤكد نجاحها
ننتقل للمرحلة 4 (الأيقونة والاسم الاحترافي) كما اتفقنا.
