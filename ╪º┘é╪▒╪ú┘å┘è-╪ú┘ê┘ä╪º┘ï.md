# إصلاح الفجوة: تصدير التقارير/الطباعة على الهاتف

## المشكلة المؤكَّدة
`printRtlReport()` (تستخدمه كل دوال تصدير PDF في التطبيق) كانت تعتمد على `window.open('', '_blank')` ثم `window.print()` — نمط متصفح كمبيوتر كلاسيكي **غير موثوق إطلاقاً داخل WebView أندرويد**: `window.open()` غالباً يُرجع `null` هناك (نفس نوع القيد الذي واجهناه سابقاً مع مشكلة النوافذ المتعددة في Electron)، وحتى لو نجح، `window.print()` لا يُشغّل نظام الطباعة الحقيقي لأندرويد بشكل موثوق من داخل WebView مُقيَّد.

## الإصلاح
على الهاتف تحديداً: يُكتَب التقرير كملف HTML حقيقي عبر `@capacitor/filesystem` (نفس المكتبة المُستخدَمة والمُختبَرة سابقاً لتصدير النسخة الاحتياطية)، ثم تُفتَح قائمة مشاركة أندرويد الأصلية عبر `@capacitor/share`. من هناك يستطيع صاحب المحل فتح الملف في متصفح حقيقي (كروم)، والذي يملك ميزة طباعة/حفظ-كـ-PDF موثوقة تماماً بخلاف WebView المُقيَّد.

على الكمبيوتر: **لا تغيير إطلاقاً** — نفس السلوك المعتاد بالضبط.

## تفصيل تقني: أعدت استخدام نفس تصميم HTML الاحترافي
لم أُعِد اختراع تصميم التقرير من الصفر — استخرجت بناء HTML الموجود مسبقاً إلى دالة مستقلة (`buildReportHtml`)، تُستخدَم الآن في المسارين (الطباعة على الكمبيوتر، والمشاركة على الهاتف) بنفس التصميم والألوان بالضبط.

## تحسين منهجية الفحص في هذه الجولة
اكتشفت أن أداة الفحص اليدوية التي بنيتها لنفسي لتتبّع توازن الأقواس عبر هذه المحادثة الطويلة **لا تتعرّف على التعبيرات النمطية (regex)** بشكل صحيح — ما أعطى نتيجة "غير متوازن" خاطئة على هذا الملف بسبب `.replace(/"/g, ...)`. بدل تجاهل هذا أو الوثوق بأداتي الخاصة، استخدمت **مترجم TypeScript الحقيقي (`tsc`)** للتحقق، وأكّد أن الملف سليم صياغياً 100% (الأخطاء الظاهرة كلها متوقعة بسبب فحص الملف بمعزل عن باقي المشروع — أنواع بيانات غير محددة صراحة، ومسارات استيراد لن تُحل إلا داخل المشروع الكامل). سأعتمد هذه الطريقة الأدق من الآن فصاعداً بدل أداتي اليدوية.

## الملف
`src/lib/pdf.ts` — استبدله بالكامل بالنسخة المرفقة.

## ⚠️ تذكير معتاد
تأكد من نسختك الحالية على GitHub قبل الاستبدال، في حال احتوت تحسينات لاحقة على نظام PDF لم أرها.

## التحقق
22/22 اختبار محاسبي، وتحقق حقيقي بمترجم TypeScript (وليس أداة يدوية) هذه المرة.

## الفجوة المتبقية الوحيدة من القائمة السابقة
استيراد النسخة الاحتياطية (Import) على الهاتف — لم أختبره بعد رغم أنه على الأرجح يعمل تلقائياً (حسب توثيق Capacitor الرسمي، حقل `<input type="file">` مدعوم بشكل طبيعي داخل WebView). أخبرني إن أردت التأكد منه بالتفصيل أيضاً.
