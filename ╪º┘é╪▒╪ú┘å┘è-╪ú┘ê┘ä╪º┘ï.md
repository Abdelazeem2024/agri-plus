# إصلاح نهائي — ألوان ثابتة لا تعتمد على الوضع الليلي/النهاري

6 ملفات. بدل محاولة مطابقة الألوان بين الوضعين، جعلت هذه العناصر تحديداً **بلون واحد ثابت دائماً** (خلفية بيضاء + نص داكن)، بغض النظر عن كون التطبيق في الوضع الليلي أو العادي — فلا يوجد أي منطق شرطي يمكن أن يفشل بعد الآن.

## الملفات
`SearchSelect.tsx` (المكوّن المشترك)، `NewInvoice.tsx`، `StockReceipts.tsx`، `EditInvoice.tsx`، `RepresentativeReturns.tsx` — كل صناديق نتائج البحث المنسدلة (عملاء، أصناف) أصبحت بيضاء بنص داكن ثابت دائماً.

`Rewards.tsx` — قسم "عند دعوة صديق" أصبح بطاقة بيضاء بنص داكن ثابت دائماً، بدل التبديل بين لونين حسب الوضع.

## تحقق بصري حقيقي قبل التسليم
لم أكتفِ بوصف الكود هذه المرة — ولّدت صورة فعلية للقائمة المنسدلة بنفس القيم اللونية الحقيقية على خلفية داكنة، لأتأكد بعيني أن النص واضح تماماً قبل إرسالها لك (مرفقة في المحادثة).

## التحقق
24/24 اختبار ناجح، وفحصت صياغة كل الملفات الستة.
