# ملفات معدَّلة — الجولة الحالية

انسخ كل ملف إلى نفس المسار بالضبط في مستودع GitHub (استبدال الملف الموجود)، ما عدا صورة الفيسبوك فهي **جديدة**.

| المسار في المستودع | نوع التعديل | التفاصيل |
|---|---|---|
| `src/components/Layout.tsx` | تعديل | قسم "Developed by Raqnova" أسفل الصفحة أصبح: زر واتساب ينسخ الرقم تلقائياً عند الضغط (بدل مجرد فتح رابط)، وزر فيسبوك يفتح بطاقة داخلية منسّقة (شعار الصفحة + الاسم + زر "زيارة الصفحة") تؤدي للرابط الصحيح `facebook.com/share/1BVcf9gnmD`. كما صححت خطأ برقم واتساب كان يحتوي رقماً زائداً بالغلط في الرابط القديم. |
| `src/assets/brand/facebook-page.png` | **ملف جديد** | صورة شعار صفحة الفيسبوك التي أرسلتها، تُستخدم داخل بطاقة الفيسبوك الجديدة |
| `src/db/storage.ts` | تعديل | كلمة مرور صفحة الأرباح الافتراضية أصبحت **1234** (كانت فارغة، ما يفرض على المستخدم تعيينها يدوياً أول مرة). تعديلها لاحقاً من داخل صفحة الأرباح يُحدّث نفس القيمة المستخدمة في تأكيد "حذف جميع البيانات" تلقائياً — الآلية كانت مربوطة بالفعل بين الاثنين في الكود، فقط القيمة الافتراضية كانت فارغة. |

## ملاحظات
- تغيير كلمة مرور الأرباح من صفحة الأرباح (زر "تغيير كلمة السر") يُحدّث تلقائياً كلمة مرور تأكيد حذف جميع البيانات في الإعدادات، لأن الحقلين في الكود هما نفس القيمة (`profitPassword`) — هذا الربط موجود ومُتحقَّق منه في `AppContext.tsx` (لم يحتج تعديلاً، فقط القيمة الافتراضية في `storage.ts`).
- لا حاجة لأي خطوة يدوية إضافية بعد النسخ — `npm run build` يلتقط الصورة الجديدة تلقائياً.
