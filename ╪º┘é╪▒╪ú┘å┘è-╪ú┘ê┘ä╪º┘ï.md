# بناء تطبيق الأندرويد بالكامل من الهاتف — بدون كمبيوتر إطلاقاً

## الفكرة
بدل أن تثبّت Android Studio على جهاز كمبيوتر، هذا الملف يجعل **خوادم GitHub المجانية نفسها** تقوم بكل خطوات البناء (تثبيت الحزم، تجهيز أندرويد، بناء APK) تلقائياً — أنت فقط تضغط زراً وتنتظر، وتُحمِّل الملف الجاهز.

## الملف
`.github/workflows/build-android.yml` — ضعه بالضبط في هذا المسار في مستودعك (أنشئ المجلدات `.github` ثم `workflows` بداخله إن لم تكونا موجودتين).

**طريقة إنشاء مجلد متداخل عبر واجهة GitHub مباشرة:** عند الضغط على "Add file → Create new file"، اكتب المسار الكامل دفعة واحدة في خانة اسم الملف:
```
.github/workflows/build-android.yml
```
سينشئ GitHub المجلدين تلقائياً بمجرد كتابة الشرطتين المائلتين.

## طريقة التشغيل بالكامل من الهاتف (خطوة بخطوة)

1. افتح متصفح هاتفك واذهب لمستودعك على GitHub.
2. اضغط تبويب **"Actions"** أعلى الصفحة.
3. من القائمة على اليسار، اختر **"بناء تطبيق أندرويد (Agri Plus)"**.
4. ابحث عن زر **"Run workflow"** (عادة بجانبه سهم صغير ▾) واضغطه، ثم أكّد بالضغط على الزر الأخضر الذي يظهر.

   > **إن لم يظهر لك الزر** (بعض متصفحات الهاتف تُخفيه في الوضع المبسّط): افتح قائمة خيارات المتصفح (⋮ عادة أعلى يمين الشاشة) وفعّل خيار **"نسخة سطح المكتب" / "Desktop site"**، ثم أعد تحميل الصفحة — سيظهر الزر بوضوح.

5. ستظهر عملية جديدة بدائرة صفراء 🟡 (تعني: قيد التشغيل الآن). اضغط عليها لمتابعة التقدّم مباشرة.
6. انتظر — يستغرق البناء الكامل عادة **5 إلى 10 دقائق**. يمكنك إغلاق المتصفح والعودة لاحقاً، العملية تستمر على خوادم GitHub بغض النظر عن هاتفك.
7. عند اكتمالها بنجاح (دائرة خضراء ✓)، افتحها وانزل لأسفل الصفحة حتى تجد قسم **"Artifacts"**.
8. اضغط على **"AgriPlus-Android-APK"** — سيُحمَّل ملف مضغوط صغير يحتوي ملف الـ APK بداخله مباشرة على هاتفك.
9. فك ضغط الملف (أي تطبيق "فك ضغط" مجاني من متجر التطبيقات يفي بالغرض)، وستجد `app-debug.apk` بداخله — هذا هو الملف الذي ترفعه على الموقع الذي تريده.

## ملاحظة مهمة عن نوع البناء
هذا الإعداد يبني نسخة **"Debug"** (نسخة اختبار) — وهي كافية تماماً للتجربة والرفع على مواقع مثل التي ذكرتها. عندما يصبح التطبيق جاهزاً فعلياً للنشر الرسمي على متجر Google Play، نحتاج خطوة إضافية منفصلة (توقيع رقمي للنسخة النهائية "Release") — أخبرني عند الوصول لتلك المرحلة وسأجهزها لك.

## إن ظهر أي خطأ أحمر ✗ أثناء التشغيل
اضغط على الخطوة التي فشلت (ستكون محددة بعلامة ✗ حمراء) لترى تفاصيل الخطأ، وانسخ لي النص كاملاً — سأشخّصه فوراً.
