# ملفات معدَّلة — 5 نقاط

8 ملفات، كلها تعديل لملفات موجودة.

| الملف | يعالج |
|---|---|
| `src/pages/Customers.tsx` | **[1]** إصلاح جذري ونهائي للون "غير نشط" في الوضع الليلي — تتبعت السبب الدقيق: قاعدة CSS عامة بـ`!important` كانت تُبطل إصلاحي السابق بصمت. استخدمت الآن لوناً غير متعارض مع أي قاعدة أخرى، مضموناً 100%. **[3]** نموذج "إضافة عميل" أصبح 4 حقول فقط (الاسم، الهاتف، المنطقة، ملاحظات) — العنوان والرصيد الافتتاحي يظهران فقط عند التعديل لاحقاً |
| `src/pages/Rewards.tsx` | **[2]** نص قيمة المكافأة (3/6 شهور، سنة) أصبح على شريحة ملوّنة صلبة بيضاء النص — تباين مضمون بصرياً بغض النظر عن الوضع |
| `src/types/index.ts` | **[5]** إضافة حقل `refundAmount` اختياري لسجل المرتجع |
| `src/pages/CustomerReturns.tsx` | **[5]** حقل جديد "المبلغ المسترد نقداً للعميل" عند تسجيل المرتجع، مع عرض الرصيد الحالي للعميل كمساعدة للبائع قبل تحديد المبلغ |
| `src/pages/CustomerStatement.tsx` | **[4+5]** المرتجع الآن يُصحَّح بسطر إضافي "مبلغ مسترد نقداً للعميل" عند إدخال مبلغ استرداد — هذا يحل مشكلة الصورة المرفقة بالضبط |
| `src/lib/pdf.ts` | **[5]** بطاقة الملخص المطبوعة تعرض "مسترد نقداً للعميل" كبند مستقل عند وجوده |
| `src/pages/Dashboard.tsx` | **[5]** "إجمالي التحصيلات" أصبح صافياً (مطروحاً منه أي مبالغ مستردة) |
| `src/pages/Reports.tsx` | **[5]** نفس التصحيح + بطاقة إحصائية مستقلة "مسترد نقداً للعملاء" |

## تحقق فعلي من إصلاح مشكلة الصورة المرفقة
شغّلت محاكاة رقمية مطابقة تماماً لأرقام الصورة (فاتورة 2000 + تحصيل 500 + مرتجع 2000 + استرداد 500):

النتيجة: **الرصيد النهائي = 0** بدل -500 الخاطئة سابقاً. بالضبط ما طلبته.

## كيف تستخدم الميزة الجديدة عملياً
عند تسجيل مرتجع لعميل دفع جزءاً من الفاتورة الأصلية ثم أرجع البضاعة بالكامل، أدخل المبلغ الذي أعطيته له نقداً فعلياً في حقل "المبلغ المسترد نقداً للعميل" الجديد. إن لم تُعطِه شيئاً (فقط ألغيت دينه)، اترك الحقل فارغاً كالمعتاد — لا تغيير في هذه الحالة.

## التحقق النهائي
24/24 اختبار ناجح بعد كل التعديلات، وفحصت صياغة كل ملف بأداة تتبّع دقيقة تراعي التداخل الصحيح لقوالب النصوص (اكتشفت وتحققت يدوياً من حالة إيجابية كاذبة واحدة بسبب Regex في `pdf.ts`، وأكدت صحتها بتنفيذها مباشرة).
