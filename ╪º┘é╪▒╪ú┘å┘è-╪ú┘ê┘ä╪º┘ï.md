# النسخ الاحتياطي التلقائي اليومي — 4 ملفات

| الملف | نوع التعديل |
|---|---|
| `electron/backup.cjs` | **ملف جديد** — كل منطق النسخ الاحتياطي والتنظيف |
| `electron/main.cjs` | تعديل — ربط الفحص التلقائي بالإقلاع + كل ساعة، وإضافة معالجات IPC |
| `electron/preload.cjs` | تعديل — كشف الدوال الجديدة للواجهة |
| `src/pages/Settings.tsx` | تعديل — قسم جديد كامل في الإعدادات لإدارة الميزة |

## كيف تعمل الميزة
- **منفصلة تماماً** عن زر "تصدير JSON" اليدوي — كلاهما يعمل بشكل مستقل.
- تختار مجلداً مرة واحدة من داخل الإعدادات (زر "اختيار مجلد الحفظ" يفتح نافذة اختيار مجلد حقيقية من نظام التشغيل).
- تفعّل المفتاح (Toggle) — من هذه اللحظة، يأخذ البرنامج نسخة كاملة تلقائياً **مرة واحدة كل يوم**، بمجرد أن يكون البرنامج مفتوحاً في ذلك اليوم (يتحقق عند الإقلاع، وكل ساعة بعدها طالما البرنامج شغّال، حتى يعمل تلقائياً لو بقي مفتوحاً طوال الليل بلا إعادة تشغيل).
- كل نسخة تُسمّى تلقائياً: `agriplus-auto-backup-YYYY-MM-DD.json`.
- بعد كل نسخة ناجحة، يحذف البرنامج تلقائياً أي نسخة (من نفس النمط فقط، لا يلمس أي ملف آخر بالمجلد) أقدم من **14 يوماً**.
- زر "أخذ نسخة الآن" لاختبار الإعداد فوراً دون انتظار.
- رسالة حالة صغيرة أسفل القسم توضح تاريخ آخر نسخة ونتيجتها.

## اختبار حقيقي أجريته قبل التسليم
شغّلت السيناريو كاملاً فعلياً (وليس نظرياً): إعداد أولي → أول نسخة تُنشأ بنجاح → محاولة ثانية لنفس اليوم تُرفض بشكل صحيح (لا تكرار) → محاكاة مرور يوم جديد مع وجود ملف عمره 20 يوماً → تأكدت أن الملف القديم **حُذف فعلياً** والنسخة الجديدة أُنشئت بنجاح. كل النتائج طابقت التوقع تماماً.

بعد الرفع وإعادة البناء: من الإعدادات، اختر مجلداً، فعّل المفتاح، واضغط "أخذ نسخة الآن" للتأكد.
