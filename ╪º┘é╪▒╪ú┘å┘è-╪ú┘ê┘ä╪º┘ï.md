# سهم الرجوع في كل الصفحات ✅

## القرار: حل مركزي واحد بدل التكرار في كل صفحة
بدل إضافة الزر يدوياً في كل صفحة (تكرار عرضة للخطأ ونسيان صفحات)، أضفته في `TopBar.tsx` — المكوّن الوحيد المشترك الذي يظهر أعلى **كل** صفحات التطبيق. هذا يضمن ظهوره في كل مكان تلقائياً، بما في ذلك أي صفحة جديدة تُضاف مستقبلاً، بدون أي جهد إضافي.

## السلوك
- **يظهر في كل الصفحات ما عدا الرئيسية** (لا فائدة من سهم رجوع في الصفحة الرئيسية، لا يوجد "قبلها" منطقياً).
- يستخدم سجلّ التصفح الفعلي (نفس مبدأ زر الرجوع في المتصفح) — يعيدك بالضبط للصفحة التي أتيت منها.
- تحققت بصرياً (صورة فعلية) من ظهوره/اختفائه بشكل صحيح.

## بخصوص التقارير وكشوف الحساب تحديداً
فحصت الصفحات الثلاث التي ذكرتها: `CustomerStatement.tsx` و`RepresentativeStatement.tsx` كان لديهما بالفعل سهم رجوع مخصص خاص بهما (يعيدك لصفحة العميل/المندوب تحديداً). أما **`Reports.tsx` (التقارير) لم يكن بها أي سهم رجوع إطلاقاً** — الإصلاح المركزي الجديد يغطّي هذه الفجوة الحقيقية مباشرة.

## الملف
`src/components/TopBar.tsx` — استبدله بالكامل (يحتوي أيضاً على البحث الذكي المبني سابقاً، لم يتأثر بأي شيء).

## التحقق
فحصته بمترجم TypeScript الحقيقي (صفر أخطاء)، وتحققت بصرياً من ظهور/اختفاء السهم بشكل صحيح، و22/22 اختبار محاسبي ناجح.
