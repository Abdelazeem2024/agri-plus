# الأرشيف الشامل النهائي — تطبيق الهاتف (المراحل 1-6 كاملة)

هذا هو الأرشيف الوحيد الذي تحتاجه لكل ما بنيناه في مسار تطبيق الهاتف — 25 ملفاً + مقتطف واحد لـ`package.json`، بحالتها النهائية المُدمَجة (كل ملف يحتوي كل الإصلاحات المتراكمة عليه من كل المراحل، وليس إصلاحاً واحداً فقط).

## ملخص كل مرحلة

### المرحلة 1: الأساس
`Sidebar.tsx` · `Layout.tsx` · `TopBar.tsx` · `capacitor.config.ts` — قائمة جانبية تتحول لدرج منزلق على الهاتف، بدون أي تغيير على سلوكها في نسخة الكمبيوتر. **مُختبَرة فعلياً من طرفك — البرنامج يعمل بنجاح.**

### البناء التلقائي (بدون كمبيوتر)
`.github/workflows/build-android.yml` — يبني APK تلقائياً على خوادم GitHub، تحمّله من هاتفك مباشرة. **مُختبَر فعلياً — نجح البناء.**

### المرحلة 2: الجداول والنوافذ المنبثقة
12 صفحة (`Collections`، `CustomerReturns`، `Customers`، `EditInvoice`، `Invoices`، `NewInvoice`، `Products`، `RepPayments`، `Reports`، `RepresentativeReturns`، `Representatives`، `StockReceipts`) + `Profits.tsx` — جداول قابلة للتمرير الأفقي، ونوافذ منبثقة قابلة للتمرير العمودي عند ظهور لوحة المفاتيح.

### المرحلة 3: قاعدة بيانات حقيقية
`src/db/capacitorDb.ts` (جديد) + تعديلات `storage.ts` — بدل `localStorage` المحدود، قاعدة بيانات SQLite حقيقية على الجهاز. **اختبرت منطق المرونة كاملاً بمحاكاة 4 سيناريوهات، بما فيها الأهم: نقل بيانات عميل قديم بدون فقدان أي شيء.**

### المرحلة 4: الأيقونة وشاشة البداية
`assets/icon.png` (1024×1024) و`assets/splash.png`/`splash-dark.png` (2732×2732) — من نفس شعار البرنامج، بألوان الهوية البصرية نفسها. مربوطة تلقائياً في خط بناء GitHub Actions.

### المرحلة 5: التصدير والنسخ الاحتياطي
`src/lib/mobileExport.ts` (جديد) + تعديل `Settings.tsx` — استبدال أسلوب التحميل غير الموثوق داخل WebView بكتابة ملف حقيقية + مشاركة أندرويد الأصلية.

### المرحلة 6: نظام الترخيص للهاتف
`src/lib/mobileDeviceId.ts` + `src/lib/mobileLicense.ts` (جديدان) + تعديلات `AppContext.tsx`/`Settings.tsx` — **نفس أداة توليد التراخيص الحالية لسطح المكتب تصلح لتوليد أكواد الهاتف أيضاً**، فقط باستخدام معرّف الجهاز (Device ID) بدل Machine ID. اختبرت التطابق الحسابي **بايتاً بايت** بين تنفيذ الهاتف (Web Crypto API) وتنفيذ سطح المكتب (Node crypto)، ثم اختبرت 4 سيناريوهات تفعيل كاملة من طرف لطرف — كلها نجحت.

## ⚠️ نقطة تحتاج تأكيدك أنت تحديداً
نظام ترخيص الهاتف مبني ليطابق خوارزمية `HMAC-SHA256` الموجودة في نسخة `electron/license.cjs` التي فحصتها مباشرة في بيئتي. **إن كانت نسختك الفعلية الحالية على GitHub تستخدم خوارزمية مختلفة** (تطورت في جلسة سابقة لم تعد متاحة لي بعد إعادة تشغيل بيئتي أكثر من مرة)، أخبرني فوراً بمحتوى ملفك الحالي لأُطابق ملفات الهاتف معه بدقة قبل استخدامها فعلياً.

## ما تم اختباره فعلياً (حقيقي، وليس افتراضاً)
- منطق التحميل/الحفظ لقاعدة بيانات الهاتف — 4 سيناريوهات محاكاة كاملة.
- التطابق الحسابي بين HMAC الهاتف وHMAC سطح المكتب — بايتاً بايت، 3 حمولات.
- التحقق من كود ترخيص من طرف لطرف — 4 سيناريوهات (دائم صحيح، سنوي صحيح، جهاز خاطئ، توقيع مزوَّر).
- ترميز WAV للصوت (من مرحلة سابقة) — بايتاً بايت.
- سلامة صياغة **كل** الملفات الـ25 — بأداة تتبّع دقيقة تراعي تداخل الأقواس والقوالب.
- 22/22 اختبار محاسبي ناجح على الحالة النهائية الكاملة بعد دمج كل شيء.

## ما لم يُختبَر (بصراحة كاملة)
- التشغيل الفعلي لمكتبة `@capacitor-community/sqlite` على جهاز أندرويد حقيقي.
- دقة حساب `Device.getId()` الفعلي من `@capacitor/device` على جهاز حقيقي.
- تجربة فعلية لتدفق التصدير/المشاركة على جهاز حقيقي.
- تجربة فعلية لإدخال كود تفعيل ونجاحه على جهاز حقيقي.

بيئتي بلا إنترنت ولا جهاز حقيقي — كل ما استطعت فعله هو التحقق من صحة كل دالة ومنطق مقابل التوثيق الرسمي ومحاكاة كل سيناريو ممكن رياضياً، لكن الاختبار الحقيقي النهائي يبقى تجربتك أنت على جهاز فعلي.

## خطوات ما بعد الرفع
1. أضف كل الملفات الـ25 في أماكنها (المسارات مطابقة لبنية `src/` الحالية عندك).
2. طبّق إضافات `package.json` من الملف المرفق.
3. Commit، ثم شغّل `build-android.yml` من تبويب Actions.
4. جرّب فعلياً: افتح التطبيق، أضف عميلاً، أغلق التطبيق وأعد فتحه (تأكد أن البيانات محفوظة)، جرّب زر التصدير، وإن توفر لديك كود تفعيل تجريبي جرّب التفعيل.
5. أخبرني بأي خطأ فعلي تراه — هذا أهم من أي اختبار أجريته أنا هنا.
