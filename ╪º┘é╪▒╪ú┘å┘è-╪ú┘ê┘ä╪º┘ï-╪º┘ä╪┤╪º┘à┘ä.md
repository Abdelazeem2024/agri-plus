# الأرشيف الشامل — كل تعديلات تطبيق الهاتف حتى الآن (18 ملفاً)

هذا الأرشيف يجمع **كل شيء** من كل الجولات السابقة الخاصة بتطبيق الهاتف في مكان واحد، بحالته النهائية المُدمَجة (تحققت أن كل ملف يحتوي كل الإصلاحات المتراكمة عليه معاً، وليس إصلاحاً واحداً فقط).

## 1) الأساس (القائمة المتجاوبة + إعدادات Capacitor)
| الملف | ماذا يفعل |
|---|---|
| `src/components/Sidebar.tsx` | قائمة جانبية تتحول لدرج منزلق على الهاتف |
| `src/components/Layout.tsx` | يدير فتح/غلق الدرج + إصلاح رقم واتساب |
| `src/components/TopBar.tsx` | زر ☰ لفتح القائمة (يظهر على الهاتف فقط) |
| `capacitor.config.ts` | إعدادات Capacitor الأساسية |
| `يُضاف-يدوياً-إلى-package.json.txt` | حزم ومكتبات Capacitor + أوامر تشغيل جديدة |

## 2) البناء التلقائي من الهاتف (بدون كمبيوتر)
| الملف | ماذا يفعل |
|---|---|
| `.github/workflows/build-android.yml` | يبني ملف APK تلقائياً على خوادم GitHub، تحمّله من هاتفك مباشرة |

## 3) إصلاحات الجداول (تمرير أفقي على الهاتف) — 12 ملفاً، مُدمَجة مع إصلاحات النوافذ حيث تتقاطع
`Collections.tsx` · `CustomerReturns.tsx` · `Customers.tsx` · `EditInvoice.tsx` · `Invoices.tsx` · `NewInvoice.tsx` · `Products.tsx` · `RepPayments.tsx` · `Reports.tsx` · `RepresentativeReturns.tsx` · `Representatives.tsx` · `StockReceipts.tsx`

## 4) إصلاحات النوافذ المنبثقة (تمرير عمودي عند ظهور لوحة المفاتيح)
مُدمَجة داخل نفس الملفات أعلاه حيث تتقاطع (`Collections`، `Customers`، `Products`، `RepPayments`، `Representatives`، `StockReceipts`)، بالإضافة إلى: `Profits.tsx` (ملف مستقل، يحتاج إصلاح النافذة فقط دون جدول).

## ✅ تحقق نهائي قبل الإرسال
- تأكدت أن الملفات المشتركة بين مرحلتي "الجداول" و"النوافذ" (مثل `Customers.tsx`) تحتوي **كلا الإصلاحين معاً** فعلياً في نفس الملف — فحصتها سطراً بسطر.
- 22/22 اختبار محاسبي ناجح على الحالة النهائية المُجمَّعة بالكامل.

## ⚠️ التذكير المعتاد (نفس الملاحظة في كل جولة)
هذه نسخة بيئتي الأساسية للملفات — راجع نسختك الحالية على GitHub قبل استبدال أي ملف، للتأكد من عدم فقدان أي تحسين سابق (خصوصاً في `TopBar.tsx` الذي يحتوي شريط بحث حياً في نسختك قد لا يكون في نسختي).

## ما لم يتم بعد (خارج نطاق هذا الأرشيف)
- ترقية قاعدة البيانات من `localStorage` إلى SQLite حقيقي متوافق مع الهاتف (مرحلة قادمة).
- نظام الترخيص لنسخة الهاتف (يحتاج تصميماً مختلفاً بالكامل).
- المساعد الصوتي على الهاتف (غير متوافق مع whisper-cli.exe الحالي، يحتاج بناءً منفصلاً).
- اختبار حقيقي فعلي على جهاز/محاكي أندرويد — لم يحدث بعد.
